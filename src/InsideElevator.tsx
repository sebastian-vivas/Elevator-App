import React, { useState } from 'react';

interface ElevatorProps {
  totalFloors: number;
}

const InsideElevator: React.FC<ElevatorProps> = ({ totalFloors }) => {

  return (
    <div>
      <button></button>
    </div>
  );
};

export default InsideElevator;